#include <iostream>
#include <vector>
using namespace std;

int main()
{
    // vector<int> ans(0);
    int t;
    cin >> t;
    while (t--)
    {
        // no of parts
        int n;
        // no of colors
        int m;
        // no of parts Bob can repaint
        int k;

        cin >> n >> m >> k;
    }
    // for(const auto& x: ans)cout<< x << '\n';
}